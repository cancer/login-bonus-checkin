{
  "name": "login-bonus-checkin",
  "version": "1.0.0",
  "private": true,
  "description": "Daily login-bonus auto checkin (HoYoLAB etc.) on Cloudflare Scheduled Workers — pluggable per-site providers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
